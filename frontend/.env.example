# =============================================================================
# BIMAP frontend
#
# Server-only origin of the internal FastAPI backend.
#
# Do NOT prefix this with NEXT_PUBLIC_.
# Browser requests use the same public Next.js origin and are rewritten
# internally by next.config.ts.
# =============================================================================

BIMAP_BACKEND_URL=http://127.0.0.1:8000
